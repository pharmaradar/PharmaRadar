{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c \"celery -A app.tasks.celery_app.celery_app worker -Q scrape --concurrency=3 --loglevel=info -n scrape@railway --max-tasks-per-child=25 --max-memory-per-child=500000\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
